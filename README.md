# Hangman Game

It's a simple Hangman Game implemented using [Elixir](https://elixir-lang.org/).

## Project Structure

```
hangman/ (projects)
  > hangman/
    > lib/
      > impl/
        > game.ex (logic)
      > hangman.ex (api)
  > dictionary/
    > dictionary.ex (api)
```
